# Spring settings
spring:
  profiles:
    active: dev

# GeTui settings
getui:
  appId: ${GETUI_APPID}
  appKey: ${GETUI_APPKEY}
  masterSecret: ${GETUI_MASTERSECRET}
  sdkUrl: "http://sdk.open.api.igexin.com/apiex.htm"
  logoUrl: ${GETUI_LOGOURL}
